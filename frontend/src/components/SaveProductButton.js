import * as React from 'react';
import Button from '@mui/material/Button';

export default function SaveProductButton() {
  return (
      <div className='save-button-space' >
        <Button variant="contained">ADD PRODUCT</Button>
      </div>

  );
}